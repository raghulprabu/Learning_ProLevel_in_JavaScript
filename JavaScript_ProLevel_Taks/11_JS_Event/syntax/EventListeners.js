//  addEventListener() allows you to attach multiple events to the same element.

element.addEventListener("eventName", handlerFunction);

button.addEventListener("click", () => console.log("Clicked"));



